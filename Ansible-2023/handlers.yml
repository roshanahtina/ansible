---
- hosts: web
  become: yes
  gather_facts: yes
  
  tasks:
  - name: Checking the HTTPD Service...
    command: systemctl status httpd.service -f
    register: httpdoutput
  - debug:
      msg: "{{ httpdoutput.stdout_lines }}"
