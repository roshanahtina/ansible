---
- hosts: localhost
  gather_facts: yes
  vars_files:
    - /etc/ansible/AWS_LAB_ANSIBLE/key_files
  
  tasks:
  - name: To get the instances information...
    ec2_instance_info:
      aws_access_key: "{{ aws_access_key }}"
      aws_secret_key: "{{ aws_secret_key }}"
      aws_region: "{{ aws_region }}"
      register: output
  - debug:
      msg: output.instances
