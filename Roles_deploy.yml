---
- hosts: ansi23
  remote_user: ansible
  become: yes
  gather_facts: true  
  
 # roles:
 #   - role_php_site_1
 #   - /home/ansible/.ansible/roles/ansiblebit.httpd
 
 tasks:
 - name: I Use
   debug:
     msg: "I Use Role"
 - name: Use Role
   include_role:
     name: /home/ansible/.ansible/roles/ansiblebit.httpd
 
 
  
