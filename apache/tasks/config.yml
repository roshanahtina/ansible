---
- name: Copying the conf file to the path
  copy:
    src: httpd.conf
    dest: /etc/httpd/conf/
  notify:
    - restart apache server
    
- name: Copying index.html file to the path
  copy:
    src: index.html
    dest: /var/www/html
  notify:
    - restart apache server
